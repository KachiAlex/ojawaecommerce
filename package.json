{
  "name": "ojawa-ecommerce",
  "version": "1.0.0",
  "scripts": {
    "dev:web": "npm run dev --prefix apps/buyer",
    "deploy:prod": "cd apps/buyer && npm run build && cd ../.. && firebase deploy --only hosting:ojawa-ecommerce",
    "deploy:staging": "cd apps/buyer && npm run build && cd ../.. && firebase deploy --only hosting:ojawa-ecommerce-staging",
    "deploy:preview": "cd apps/buyer && npm run build && cd ../.. && firebase hosting:channel:deploy test-channel"
  },
  "dependencies": {
    "firebase-admin": "^13.5.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0"
  }
}
